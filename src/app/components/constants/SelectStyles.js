export const customSelectStyles = {
  control: (p) => ({
    ...p,
    borderColor: "#d1d5db",
    padding: "0.5rem",
    borderRadius: "0.5rem",
    minHeight: "44px",
    "&:hover": { borderColor: "#10b981" },
    boxShadow: "none",
  }),
  // …other overrides (same as you had)
};